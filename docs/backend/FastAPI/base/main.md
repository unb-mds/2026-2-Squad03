# Aplicação Principal (Main)

Esta seção documenta o ponto de entrada da API do VeritasIA (`main.py`). Aqui são inicializadas as configurações do FastAPI, middlewares de segurança (CORS) e o mapeamento de todas as rotas do backend.

::: backend.app.main
    options:
      show_root_heading: false
      show_source: true
      heading_level: 2
      show_if_no_docstring: true