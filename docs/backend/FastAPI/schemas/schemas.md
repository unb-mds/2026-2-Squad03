# Schemas de Dados (Notícias)

Esta página documenta os modelos de validação de dados construídos com Pydantic. Estes schemas atuam como uma camada de segurança e formatação entre as requisições HTTP (como o envio de dados pelo Scraper) e as respostas da API, garantindo que tudo siga o padrão esperado.

::: backend.app.schemas.noticia
    options:
      show_root_heading: false
      show_source: true
      heading_level: 2
      show_if_no_docstring: true